'use client';

import { ThemeToggle } from '@/components/shared/ThemeToggle';

/** Compact theme control for marketing pages (no app sidebar). */
export function MarketingThemeToggle() {
  return <ThemeToggle compact />;
}
