# Production env template (copy secrets from .env.prod). Next.js loads this in production builds.
# Model used only for POST /api/jobs/analyze (job fit analysis); CV/CL generation uses ANTHROPIC_MODEL
CV_ANALYZER_API_MODEL=claude-haiku-4-5-20251001
